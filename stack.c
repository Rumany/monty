#include "monty.h"

/**
 * f_stack - switches the mode to stack (add to the top)
 * @head: pointer to the stack head
 * @counter: line number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
